import type { AnyTRPCRouter } from "@trpc/server";
import { type TrpcPanelExtraOptions } from "./parse/parseRouter";
export type Info = {
    title?: string;
    description?: string;
};
export type RenderOptions = {
    url: string;
    cache?: boolean;
    meta?: Info;
} & TrpcPanelExtraOptions;
export declare function renderTrpcPanel(router: AnyTRPCRouter, options: RenderOptions): string;
